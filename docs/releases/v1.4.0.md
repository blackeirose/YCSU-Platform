# YCSU Platform v1.4.0 — Mobile Email OTP Sign-In

**Status: RELEASED v1.4.0.** Desktop and physical-mobile acceptance passed on 2026-09-11. GitHub published 2026-09-12 00:30:45 UTC.

## Scope and implementation

Mobile Magic Links may open Gmail/Mail's embedded browser or another browser context, so the original MAIN browser does not acquire that session. The feature lets users enter an emailed six-digit code in the browser where they started. It uses the existing Supabase Auth SDK and exact server-side owner UUID authorization. No Registry architecture, permission or other-app changes.

Request: signInWithOtp, shouldCreateUser:false, explicit MAIN emailRedirectTo. Verify: verifyOtp with email/token/type=email. One field supports leading zeroes, numeric keypad, one-time-code autofill, paste and Enter. Resend uses a 60-second cooldown. Safe errors cover invalid/expired/used codes, network failure, rate limits and non-owner sessions. Standard SDK persistence and Magic Link callback support remain. Old verification results cannot resurrect a superseded session.

## Starting configuration and completed setup

Supabase project fzydsnxxcdllkjxwdiwn was directly inspected, not inferred from another project:

- Custom SMTP OFF; sender address/name, host and username empty. No credential was read or displayed; no configuration was saved.
- Existing default Magic Link template has only ConfirmationURL and no Token. At that starting checkpoint, default SMTP blocked template customization.
- Email provider enabled; OTP length 8, expiry 3600 seconds.
- Shared Site URL https://tracker.ycsu.cc/; allowed redirects include both https://tracker.ycsu.cc/ and https://main.ycsu.cc/. Keep them unchanged; MAIN already requests its own redirect.

Original setup requirement (now completed): connect the owner's existing Resend Custom SMTP to this exact project using the dashboard. Set verified sender address/name, host smtp.resend.com, port 465, username resend; confidential authentication stays in the dashboard. Then set Email OTP length to 6, retain expiry unless separately changed, and apply `supabase/templates/magic-link.html` as the Magic link or OTP body (subject: Your YCSU sign-in code). It includes both Token and ConfirmationURL; do not change unrelated templates. Completed: owner configured Resend; enabled/host checked without secrets. OTP length 6 and the reviewed subject/body were saved and reloaded. One authorized request returned 200 and YuCheng confirmed a six-digit email.

## Acceptance matrix

| Requirement | Evidence / state |
|---|---|
| Existing release | v1.2 and v1.3 published independently; v1.3 retained as secure rollback baseline |
| API compatibility | Current Supabase docs and actual pinned 2.116.0 SDK methods inspected |
| Build / regression | Build and all 17 tests PASS, including original Registry/reorder/security cases |
| Owner authorization | Existing backend untouched; non-owner tests deny protected data and writes |
| Request/verify/errors | Local tests PASS for duplicate request, cooldown/resend, paste/leading zero, invalid/expired/used code, network and rate limit |
| Session / logout | Existing hydration and cross-tab tests PASS; new delayed OTP vs sign-out and identity-change integration tests PASS |
| Browser UI | Local simulated fixture PASS at 390/820/1440: no horizontal overflow, 16px email/24px OTP, 48px primary buttons, Enter, wrong-code feedback, owner controls then immediate logout removal |
| Independent review | PASS for source after OTP/session race fix; not a claim of SMTP acceptance |
| Real email delivery | PASS: Resend request 200; owner confirmed six-digit code. YuCheng also confirmed fresh desktop request/code/login after signing out |
| Actual mobile email-app switch and return | PASS: YuCheng confirmed six-digit original-browser login, product links and reorder on a physical mobile device |
| Valid session after full browser reopen | PASS: YuCheng confirmed mobile refresh/reopen remains signed in; a new desktop production tab restored the existing owner with 24 card links |
| Real expired code / second production user | Local error/non-owner paths tested; no unsafe production test account created |
| Production candidate / tag | Deploy 6aa49a34fa3641ec6784ecbb ready, reviewed asset hashes match; guest-safe API/DOM pass. v1.4.0 published at accepted merge 8e3052382dfec453ba92444a5e08d613a4366fc6 |

Supabase advisors remain the existing intentional [RLS-with-no-policy INFO](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy) and pre-existing [leaked-password-protection WARN](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection). This candidate changes neither DB access nor password policy.

## Release and recovery

SMTP, six-digit configuration, reviewed email template, delivery, fresh desktop OTP and physical mobile acceptance are complete. Desktop existing-session restoration is verified. v1.4.0 was published separately; MAIN Registry now reports v1.4.0 / github-release. Final dist deployment and canonical Drive synchronization are recorded below. Keep the v1.3 secure deployment available for recovery. No database, Edge Function, other-app or authorization changes are part of v1.4.

Remaining test limits: expired/reused-code, network/rate-limit and non-owner denial paths were tested locally; no second production account or deliberately expired production code was created. Production desktop/mobile success and mobile persistence are owner-confirmed. Existing-session restoration was also observed directly in desktop Chrome.

Candidate deployment source: 42d25f191de950730343697b9b065ae45657d241. PR: https://github.com/blackeirose/YCSU-Platform/pull/1. Six-digit delivery and physical mobile acceptance are confirmed. No runtime changes followed independent review or the accepted candidate deployment.


Release: https://github.com/blackeirose/YCSU-Platform/releases/tag/v1.4.0. Tag commit: `8e3052382dfec453ba92444a5e08d613a4366fc6`. MAIN Registry version update read back successfully; the only public metadata change was MAIN version. User-selected order is preserved in the refreshed public-safe snapshot.


## Final production verification

Final production deployment: `6aa49d8b2ef630b94b973b22`, ready/published 2026-09-12 00:32:12 UTC from source `bc3f715` (accepted runtime unchanged). All 18 public runtime files match dist SHA256. Public Registry validates eight products and MAIN v1.4.0; direct protected-column read returns 401; source docs, email template and former full snapshot return 404. Clean guest DOM has 8 cards, 0 product anchors and 0 card buttons; owner reload has 24 anchors and 8 reorder buttons. Both show v1.4.0 with zero console errors; guest has no overflow or Netlify badge. Only MAIN version changed in the before/after public metadata comparison. User-selected ordering is preserved.

v1.4.0 is now the accepted current production release. Secure v1.3 rollback deployment: `6aa489b6f0b74e7b1f8b75f6`; retain RLS/privilege denial, public-safe artifacts and the compatible email link fallback during recovery.
