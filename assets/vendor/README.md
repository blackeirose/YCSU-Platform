# Pinned browser dependency

`supabase-2.116.0.js` is the unmodified UMD distribution of `@supabase/supabase-js` 2.116.0, downloaded from https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.116.0/dist/umd/supabase.js. Its MIT license is included as `SUPABASE-LICENSE`.

It loads only when owner sign-in is used or an owner session is restored. Public Registry rendering has no SDK dependency. Credentials are not included. Refresh this vendored version explicitly; there is no deployment-time download or frontend build step.
